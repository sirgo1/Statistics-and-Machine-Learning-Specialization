result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait" "\n")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("Connecting...please wait", "\n")
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
dbClearResult(result)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Connecting...please wait", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$region <- ifelse(result$size < mean(result$size),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$region, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
query <- sprintf("SELECT locality, COUNT(*) AS size
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
##########################
get_locality_by_practiceid <- function() {
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
gp_wales <- dbGetQuery(connection,
"SELECT practiceid, COUNT(*) AS size
FROM gp_data_up_to_2015
GROUP BY practiceid")
##########################
get_locality_by_practiceid <- function() {
cat("Select GP")
cat(gp_wales$practiceid)
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
cat(unique(gp_wales$practiceid))
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
cat(unique(gp_wales))
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
##########################
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
cat(unique(gp_wales))
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
cat(gp_wales)
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
print(gp_wales)
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
print(gp_wales$practiceid)
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
##########################
get_locality_by_practiceid <- function() {
cat("Select GP", "\n")
print(gp_wales$practiceid)
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
cat("You're welcome!  \n ")
print(gp_wales$practiceid)
cat("To proceed kindly enter a practiceid from the list above  \n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait...querying the database", "\n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and belong to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!  \n ")
print(paste(gp_wales$practiceid, collapse = "\n"))
cat("To proceed enter a practiceid \n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!  \n ")
cat(print(gp_wales$practiceid), "\n")
cat("To proceed enter a practiceid \n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!  \n ")
cat(gp_wales$practiceid, "\n")
cat("To proceed enter a practiceid \n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!  \n ")
print(gp_wales$practiceid)
cat("To proceed enter a practiceid \n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!",  "\n ")
print(gp_wales$practiceid)
cat("To proceed enter a practiceid", "\ n ")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
get_locality_by_practiceid <- function() {
cat("You're welcome!",  "\n ")
print(gp_wales$practiceid)
cat("To proceed enter a practiceid", "\n")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
##########################
get_locality_by_practiceid <- function() {
cat("You're welcome!",  "\n ")
print(gp_wales$practiceid)
cat("To proceed enter a practiceid", "\n")
practice_id <- readline(prompt = "Enter the practiceid: ")
cat("Please wait... \n")
query <- sprintf("SELECT locality, COUNT(*) AS count
FROM gp_data_up_to_2015 WHERE practiceid = '%s'
GROUP BY locality", practice_id)
result <- dbGetQuery(connection, query)
result$category <- ifelse(result$count < mean(result$count),
"Small Practice", "Large Practice")
if (nrow(result) > 0) {
cat("The practice with ID", practice_id, "belongs to locality", result$locality,
"and to the category", result$category, "\n")
} else {
cat("Practice ID", practice_id, "not found in the database.\n")
}
}
# Assuming you have a valid 'connection' object established
get_locality_by_practiceid()
setwd("C:/Users/Hd Okwa Drilling/Desktop/Maratuka")
setwd("C:/Users/Hd Okwa Drilling/Documents/JHU Data Science Course materials/Regression")
